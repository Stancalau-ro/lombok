apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {

    compile "org.projectlombok:lombok:$lombokVersion"

    compile group: 'com.github.peterwippermann.junit4', name: 'parameterized-suite', version: '1.1.0' //TODO version

    testCompile "junit:junit:$junitVersion"
}

